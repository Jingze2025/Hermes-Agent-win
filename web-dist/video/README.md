# Hermes final bundled video

Final web UI video asset:

- `doubao.mp4` → served at `/video/doubao.mp4`

The final left sidebar uses only this video. Older background/sidebar demo videos were intentionally removed from the final package.
